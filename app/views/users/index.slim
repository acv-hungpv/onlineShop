= render 'shared/page_title', title: "showing all users"
div[align="center"]
  = will_paginate
  - @users.each do |user|
    ul.listing

    .row
      .wel.col-md-4.col-md-offset-4

    li
      = user.name.capitalize
    li
      = user.email.capitalize
= will_paginate